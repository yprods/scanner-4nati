# קובץ הגדרות nginx לדומיין
# העתק ל: /etc/nginx/sites-available/scanner.yprods.com

server {
    listen 80;
    server_name scanner.yprods.com;

    # הגדרת גודל מקסימלי להעלאת קבצים
    client_max_body_size 10M;

    # Proxy לשרת Node.js
    location / {
        proxy_pass https://localhost:3443;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # הגדרות timeout
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# אחרי יצירת SSL, certbot יוסיף אוטומטית:
# server {
#     listen 443 ssl;
#     server_name scanner.yprods.com;
#     ssl_certificate /etc/letsencrypt/live/scanner.yprods.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/scanner.yprods.com/privkey.pem;
#     ...
# }

